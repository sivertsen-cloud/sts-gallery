name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Adjust if you're using a different default branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Replace environment variables in files
      run: |
        # Use sed to replace placeholders with secrets
        sed -i 's/YOUR_CONTENTFUL_SPACE_ID/${{ secrets.CONTENTFUL_SPACE_ID }}/g' index.html
        sed -i 's/YOUR_CDA_ACCESS_TOKEN/${{ secrets.CONTENTFUL_CDA_ACCESS_TOKEN }}/g' index.html
        sed -i 's/YOUR_PREVIEW_API_ACCESS_TOKEN/${{ secrets.CONTENTFUL_PREVIEW_API_ACCESS_TOKEN }}/g' index.html

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./  # Assuming your HTML files are in the root of the repository
